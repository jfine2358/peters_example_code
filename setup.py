from setuptools import setup, find_packages

setup(
    name = "peters_example_code",
    author = "peter",
    version = 0,
    dependency_links = (),
    install_requires = ['numpy', 'matplotlib'],
    scripts = [],
    packages=find_packages(),
    )
